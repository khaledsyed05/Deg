# Contradiction Resolution Log
> Platform: دق احجزلي / Daq Ehjezly
> Prepared by: Principal Solution Architect Review
> Date: 2026-04-04

---

| ID | Topic | Source A | Source B | Conflict Summary | Final Decision | Resolution Basis | Confidence | Follow-up? |
|----|-------|----------|----------|-----------------|----------------|-----------------|-----------|-----------|
| CR-001 | Venue sport model | LD-009: "Venue supports multiple sports via pivot table `venue_sport_categories`" | LD-054: "Venue supports ONE sport only — pivot removed. `venues.sport_category_id` direct FK." | LD-009 created the multi-sport pivot. LD-054 explicitly reverts this. Both are present in architecture doc. | LD-054 wins. Single sport per venue. `sport_category_id` direct FK on `venues`. No pivot. | LD-054 is newer and explicitly labelled as a locked decision superseding LD-009. The phrase "اعمل قرار" confirms intent to reverse the earlier assumption. | HIGH | No |
| CR-002 | Category model for venues | LD-005 / LD-009: `sport_categories` table used for venue categorization | LD-063: `venue_categories` is the new category system for venues. `sport_categories` demoted to Events tab only. | Two category systems exist in the docs. `sport_categories` is referenced for venue filtering in multiple places including LD-011, LD-013, and several API definitions. | LD-063 wins. `venue_categories` (with `type` field) is the venue categorization system. `sport_categories` exists only for Events tab football display. All venue-facing category references must use `venue_categories`. | LD-063 is the most recent explicit decision and is grounded in confirmed scalability requirement. | HIGH | API filter param naming must align — `category_id` not `sport_category_id` in public filters. |
| CR-003 | `bookings.sport_category_id` relevance | Multiple sections reference `sport_category_id` as required on bookings for multi-sport venues | LD-054 removes multi-sport. LD-063 makes venue categories generic. | If a venue has one category (which may not be a sport), the `sport_category_id` snapshot on bookings is misleading. | Field renamed to `category_id` on bookings (nullable). Populated from `venue.category_id` at booking time. NULL for non-sports bookings. | Follows directly from LD-054 + LD-063 combination. | HIGH | No |
| CR-004 | Commission table duplication | Schema file has `commission_configs` defined twice — once in the new tables section and once in an older dropped-tables/settlement area | Duplicate definitions with slightly different column sets | Schema has two `commission_configs` block definitions with differing columns. | The version in the dedicated `### commission_configs` block (with `apply_as` enum, `cancellation_fee`, `effective_from`) is canonical. The older duplicate is removed. | More complete and aligned with LD-061 narrative. | HIGH | No |
| CR-005 | Settlement table naming | Schema has both `club_settlements` (old) and `settlements` + `settlement_items` (new) | Two different table name sets for the same concept | Both exist in schema file. `club_settlements` is an older simpler version. | `settlements` + `settlement_items` is canonical. `club_settlements` is dropped. | LD-062 explicitly defines the two-table model. Older single table was an intermediate draft. | HIGH | No |
| CR-006 | `reviews` target entity | LD-025: "Rating is on the Venue level." Venue entity has `avg_rating` and `reviews_count`. | LD-057: "Review is at Club level — not venue level." | Both decisions are in the same architecture doc, with LD-057 being newer. | LD-057 wins. Reviews target clubs. `reviews.club_id` FK, no `reviews.venue_id`. `clubs.avg_rating` is the display rating. `venues.avg_rating` is kept as a separate venue-quality metric but is NOT computed from the reviews table — it is an admin-settable or derived field (INFERENCE: set to NULL unless explicitly computed). | LD-057 is explicitly final and grounded in direct confirmation. LD-025 was an early assumption. | HIGH | `venues.avg_rating` computation source must be confirmed. Currently set to NULL unless system computes it differently. |
| CR-007 | `LD-001` vs dashboard auth | LD-001: "No email/password authentication." | LD-052: Dashboard users (club staff, super admin) authenticate with email + password. | LD-001 seems to ban all email/password. LD-052 uses it for dashboard. | No real contradiction — LD-001 applies only to PLAYER (mobile) authentication. Dashboard authentication is always email+password. Both coexist correctly under different guards. | Context resolution: LD-001 says "User authentication" referring to mobile players. LD-052 explicitly scopes to dashboard actors. | HIGH | No |
| CR-008 | OTP digits in screen analysis | Screen A03 analysis contains references to "6 digits" in some sub-sections (lines 1209, 1223 in architecture) | LD-037: OTP is 5 digits. Screen A03 header says 5 digits. | Two values present in the same screen analysis. | 5 digits. LD-037 is locked. Screen analysis was updated to 5 in header but body text was inconsistently left at 6 in some places. | LD-037 is explicitly locked and based on existing codebase. | HIGH | No |
| CR-009 | `payment_provider` enum in API section | Section 10.5 lists `payment_provider` as: `syriatel_cash`, `mtn_cash`, `fatora`, `sama_pay` — missing `wallet` | LD-045 adds `wallet` as a provider. Schema `payments.provider` includes `wallet`. | Enum in mobile constants section is incomplete. | Canonical `payment_provider` enum: `syriatel_cash`, `mtn_cash`, `fatora`, `sama_pay`, `wallet`. | LD-045 is locked. Section 10.5 was written before LD-045 was finalized. | HIGH | No |
| CR-010 | `booking_status` enum in API section | Section 10.5: `pending`, `confirmed`, `cancelled`, `completed`, `no_show` | LD-046 adds `scheduled`. Schema has `confirmed`, `scheduled`, `cancelled`, `completed`, `no_show`, `failed`. | Missing `scheduled` and `failed` in the mobile constants section. | Canonical booking status: `scheduled`, `confirmed`, `cancelled`, `completed`, `no_show`, `failed`. `pending` is NOT a valid booking status — booking is never created without payment. | LD-041 (no pending) + LD-046 (scheduled added) + schema definition. | HIGH | No |
| CR-011 | Redis / Queue / Cache conflict | Architecture mentions Redis avoidance in LD-039. Some sections reference cache drivers without specifying file vs db. | LD-039 explicitly: File cache + Database queue. No Redis. | No real conflict in final state — LD-039 resolves it. Some exploratory mentions of Redis in earlier sections were not finalized. | File cache. Database queue. No Redis. Canonical. | LD-039 is a locked confirmed decision. | HIGH | No |
| CR-012 | Multi-sport venue filtering after LD-054 | LD-011 says "filter by `sport_category_id`". LD-013 says search includes "sport" dimension. Multiple API sections use `sport_category_id` filter param. | LD-054 removes multi-sport. LD-063 introduces `venue_categories`. | Filter param naming is now wrong across API definitions. | Filter param is renamed to `category_id` everywhere in the mobile API. Values reference `venue_categories.id`. The semantics remain: user browses by category, which may be a sport or a hall type. | LD-054 + LD-063 combination. | HIGH | Mobile team must update all filter references from `sport_category_id` to `category_id`. |
| CR-013 | `venue_categories.type` enum values | Schema defines: `sports`, `hall`, `court`, `room`, `outdoor`, `other` | No other definition exists | Single definition but `court` and `hall` may overlap with `sports` for sports courts. | Canonical type enum: `sports`, `hall`, `court`, `outdoor`, `other`. Remove `room` (too granular at this stage). `court` = sports-adjacent bookable space (tennis court, basketball court). `hall` = enclosed non-sports hall (wedding, mourning, conference). | Production practicality — cleaner enum. Can be extended. | MEDIUM | Admin UI must display type in human-readable form. |
| CR-014 | LD-009 impact on `bookings.sport_category_id` business rule | Business rules section (line 4897): "For multi-sport venues: `sport_category_id` is required at booking time" | LD-054 removes multi-sport entirely | Rule references a model that no longer exists. | Rule becomes: `bookings.category_id` is always set from `venue.category_id` at booking creation time. Player never selects a category at booking time — it is auto-populated from the venue. | LD-054 explicit decision: "sport is auto-selected from venue." | HIGH | No |
| CR-015 | `venues.avg_rating` source | Architecture section 11 says `Venue.avg_rating` is updated by ReviewObserver | LD-057 says reviews are on clubs, not venues | ReviewObserver updating venue avg_rating has no data source anymore since reviews reference clubs | `venues.avg_rating` is decoupled from the review system. It is either: (a) removed, or (b) kept as an admin-managed or system-managed quality score, defaulting to NULL. CANONICAL DECISION: keep the column, default NULL, not updated by ReviewObserver. Admin can manually set it in future if needed. `clubs.avg_rating` is updated by ReviewObserver. | LD-057 + production practicality. Keeping the column costs nothing and allows future use. | MEDIUM | If venue-level reviews are ever reintroduced this column is ready. |
| CR-016 | `otp_challenges` UUID vs integer ID | Schema note says: "use the DB id as verification_request_id" (old) | Later schema update adds `uuid CHAR(36) NOT NULL UNIQUE` | Conflicting field strategy for verification_request_id | `uuid` column is canonical. `id` is internal only. `verification_request_id` returned to mobile is always the `uuid`. | Non-sequential IDs for external-facing references is correct security practice. Later schema update supersedes old note. | HIGH | No |
| CR-017 | WhatsApp OTP — blocked vs supported | Earlier session: WhatsApp blocked by Meta for Syria (+963) → removed from scope | Later session: Baileys (unofficial) re-introduced. Both present in architecture. | Two opposing decisions about WhatsApp OTP in the same document | Baileys approach is canonical (LD-048). The earlier blocking was about official Meta Business API, not Baileys WebSocket protocol. Movo Delivery confirmed as real-world proof. Risk is documented and accepted. | LD-048 final decision explicitly re-introduces Baileys with documented risks and fallback to SMS. | HIGH | No |
| CR-018 | Cancellation deduction vs cancellation commission | LD-045: "cancellation_deduction" = total amount withheld from player refund (percentage or flat) | LD-061: `commission_configs.cancellation_fee` = platform's commission on cancellation | Two overlapping cancellation fee concepts | Both exist but serve different purposes: `cancellation_deduction` (BookingSettings) = total deduction from player's wallet refund. `cancellation_commission` (commission_configs) = the portion of that deduction that goes to platform (separate from any club share). In practice for initial release: deduction = platform fee entirely (clubs get nothing from cancellation deduction). | Both LD-045 and LD-061 coexist. The `bookings.cancellation_commission` field captures the platform's share at booking time. | HIGH | No |

