# Canonical API Paths

This document is the SOURCE OF TRUTH for the path of every mobile-facing
endpoint in the `deg-ehjezli` backend. The mobile team should use these
paths exactly. Generated by reconciling `routes/api.php` (post-Sprint 2)
with `BACKEND_REQUIREMENTS.md`.

Format per row: `METHOD /api/v1/<path>` — `Controller@method` —
ownership / notes.

---

## Phase 1: Auth & Profile

- `POST /api/v1/auth/otp/send` — `Api\V1\AuthController@sendOtp`
- `POST /api/v1/auth/otp/verify` — `Api\V1\AuthController@verifyOtp`
- `POST /api/v1/auth/otp/resend` — `Api\V1\AuthController@resendOtp`
- `POST /api/v1/auth/google` — `Api\V1\AuthController@googleSignIn`
- `POST /api/v1/auth/logout` — `Api\V1\AuthController@logout`
- `POST /api/v1/auth/logout-all` — `Api\V1\AuthController@logoutAll`
- `GET  /api/v1/auth/sessions` — `Api\V1\AuthController@sessions`
- `POST /api/v1/auth/refresh` — `Api\V1\AuthController@refresh`
- `GET  /api/v1/profile` — `Api\V1\ProfileController@show`
- `PUT  /api/v1/profile` — `Api\V1\ProfileController@update`
- `POST /api/v1/profile/avatar` — `Api\V1\ProfileController@uploadAvatar`
- `DELETE /api/v1/profile/avatar` — `Api\V1\ProfileController@deleteAvatar`
- `POST /api/v1/profile/notifications` —
  `Api\V1\ProfileController@updateNotifications`
- `POST /api/v1/profile/phone-change` —
  `Api\V1\ProfileController@initiatePhoneChange`
- `POST /api/v1/profile/phone-change/verify` —
  `Api\V1\ProfileController@verifyPhoneChange`
- `POST /api/v1/devices` — `Api\V1\DeviceController@register`
- `DELETE /api/v1/devices/{token}` — `Api\V1\DeviceController@destroy`

## Phase 2: Home + Discovery

- `GET /api/v1/categories` — `Api\V1\CategoryController@index`
- `GET /api/v1/categories/{category}` —
  `Api\V1\CategoryController@show`
- `GET /api/v1/venues` — `Api\V1\VenueController@index`
- `GET /api/v1/venues/search` — `Api\V1\VenueController@search`
- `GET /api/v1/venues/nearby` — `Api\V1\VenueController@nearby`
- `GET /api/v1/venues/featured` — `Api\V1\VenueController@featured`
- `GET /api/v1/venues/popular` — `Api\V1\VenueController@popular`
- `GET /api/v1/venues/recently-viewed` —
  `Api\V1\VenueController@recentlyViewed` (auth)
- `GET /api/v1/venues/clusters` —
  `Api\V1\Geography\GeographyController@venueClusters`
- `GET /api/v1/venues/{venue}` — `Api\V1\VenueController@show`
- `GET /api/v1/venues/{venue}/availability` —
  `Api\V1\VenueController@availability`
- `GET /api/v1/venues/{venue}/reviews` —
  `Api\V1\VenueController@reviews`
- `GET /api/v1/venues/{venue}/slots` —
  `Api\V1\VenueController@availableSlots`
- `GET /api/v1/venues/{slug}/similar` —
  `Api\V1\VenueController@similar`
- `GET /api/v1/venues/{slug}/photos` —
  `Api\V1\VenueController@photos`
- `GET /api/v1/content/banners` —
  `Api\V1\Content\ContentController@banners`
- `GET /api/v1/content/featured` —
  `Api\V1\Content\ContentController@featured`
- `GET /api/v1/content/blog` — `Api\V1\Content\ContentController@blog`
- `GET /api/v1/content/tips` — `Api\V1\Content\ContentController@tips`
- `GET /api/v1/content/videos` —
  `Api\V1\Content\ContentController@videos`
- `GET /api/v1/promotions` — `Api\V1\PromotionController@index`
- `GET /api/v1/promotions/featured` —
  `Api\V1\PromotionController@featured`
- `GET /api/v1/promotions/venue/{slug}` —
  `Api\V1\PromotionController@forVenue`
- `GET /api/v1/promotions/{code}` — `Api\V1\PromotionController@show`
- `GET /api/v1/events` — `Api\V1\Event\EventController@index`

## Phase 3: Venue Detail + Booking

- `POST /api/v1/bookings/check-availability` —
  `Api\V1\BookingController@checkAvailability`
- `POST /api/v1/bookings/calculate-price` —
  `Api\V1\BookingController@calculatePrice`
- `GET /api/v1/bookings` — `Api\V1\BookingController@index`
- `GET /api/v1/bookings/upcoming` —
  `Api\V1\BookingController@upcoming`
- `GET /api/v1/bookings/past` — `Api\V1\BookingController@past`
- `GET /api/v1/bookings/{id}` — `Api\V1\BookingController@show`
- `GET /api/v1/bookings/{id}/receipt` —
  `Api\V1\BookingController@receipt`
- `POST /api/v1/bookings` — `Api\V1\BookingController@store`
- `PUT /api/v1/bookings/{id}/cancel` —
  `Api\V1\BookingController@cancel`
- `PUT /api/v1/bookings/{id}/reschedule` —
  `Api\V1\BookingController@reschedule`
- `POST /api/v1/bookings/{id}/refund` —
  `Api\V1\BookingController@refund`
- `POST /api/v1/reviews` — `Api\V1\ReviewController@store`
- `GET /api/v1/reviews/my-reviews` —
  `Api\V1\ReviewController@myReviews`
- `GET /api/v1/reviews/pending` —
  `Api\V1\ReviewController@pending`
- `PUT /api/v1/reviews/{review_id}` —
  `Api\V1\ReviewController@update`
- `DELETE /api/v1/reviews/{review_id}` —
  `Api\V1\ReviewController@destroy`
- `POST /api/v1/reviews/{review_id}/helpful` —
  `Api\V1\ReviewController@markHelpful`

## Phase: Maps + Filters + Settings

- `GET /api/v1/cities` —
  `Api\V1\Geography\GeographyController@popularCities`
- `GET /api/v1/cities/{id}` —
  `Api\V1\Geography\GeographyController@show`
- `GET /api/v1/cities/{id}/neighborhoods` —
  `Api\V1\Geography\GeographyController@neighborhoods`
- `GET /api/v1/venues/clusters` —
  `Api\V1\Geography\GeographyController@venueClusters`

## Phase: Football, Waitlist, Deals

- `GET /api/v1/football/matches/today` —
  `Api\V1\Football\MatchesController@today`
- `GET /api/v1/football/matches/upcoming` —
  `Api\V1\Football\MatchesController@upcoming`
- `GET /api/v1/football/matches/yesterday` —
  `Api\V1\Football\MatchesController@yesterday`
- `GET /api/v1/football/matches/{externalId}` —
  `Api\V1\Football\MatchesController@show`
- `GET /api/v1/football/live/matches` —
  `Api\V1\Football\LiveScoreController@index`
- `GET /api/v1/football/live/matches/{id}` —
  `Api\V1\Football\LiveScoreController@show`
- `GET /api/v1/football/live/matches/{id}/events` —
  `Api\V1\Football\LiveScoreController@events`
- `GET /api/v1/football/live/matches/{id}/lineups` —
  `Api\V1\Football\LiveScoreController@lineups`
- `GET /api/v1/football/live/matches/{id}/statistics` —
  `Api\V1\Football\LiveScoreController@statistics`
- `GET /api/v1/football/leagues` —
  `Api\V1\Football\LeaguesController@index`
- `GET /api/v1/football/leagues/{id}/standings` —
  `Api\V1\Football\LeaguesController@standings`
- `GET /api/v1/football/teams` —
  `Api\V1\Football\TeamsController@index`
- `GET /api/v1/waitlist` — `Api\V1\WaitlistController@index`
- `POST /api/v1/waitlist` — `Api\V1\WaitlistController@store`
- `DELETE /api/v1/waitlist/{waitlist}` —
  `Api\V1\WaitlistController@destroy`

## Phase 7: Tournaments + Notifications

- `GET /api/v1/events` — `Api\V1\Event\EventController@index`
- `GET /api/v1/events/{id}` —
  `Api\V1\Event\EventController@show`
- `POST /api/v1/events/{id}/register` —
  `Api\V1\Event\EventController@register`
- `GET /api/v1/events/{id}/participants` —
  `Api\V1\Event\EventController@participants`
- `GET /api/v1/events/{id}/results` —
  `Api\V1\Event\EventController@results`
- `GET /api/v1/notifications` —
  `Api\V1\NotificationController@index`
- `GET /api/v1/notifications/unread` —
  `Api\V1\NotificationController@unread`
- `PUT /api/v1/notifications/{id}/read` —
  `Api\V1\NotificationController@markAsRead`
- `PUT /api/v1/notifications/read-all` —
  `Api\V1\NotificationController@markAllAsRead`
- `DELETE /api/v1/notifications/{id}` —
  `Api\V1\NotificationController@destroy`
- `GET /api/v1/notifications/settings` —
  `Api\V1\NotificationController@getSettings`
- `PUT /api/v1/notifications/settings` —
  `Api\V1\NotificationController@updateSettings`

## Phase 8: Wallet

- `GET /api/v1/wallet/account` —
  `Api\V1\WalletController@account`
- `GET /api/v1/wallet/transactions` —
  `Api\V1\WalletController@transactions`
- `GET /api/v1/wallet/settings` —
  `Api\V1\WalletController@getSettings`
- `PUT /api/v1/wallet/settings` —
  `Api\V1\WalletController@updateSettings`
- `POST /api/v1/wallet/topup` —
  `Api\V1\WalletController@topup`
- `POST /api/v1/wallet/pay-booking` —
  `Api\V1\WalletController@payBooking` (new in Sprint 3 — atomic +
  idempotent; consumes `App\Services\Wallet\PayBookingService` and
  the `App\Support\Idempotency` primitive).
- `POST /api/v1/wallet/topup/verify` —
  `Api\V1\WalletController@verifyTopup`
- `POST /api/v1/wallet/topup/resend-otp` —
  `Api\V1\WalletController@resendTopupOtp`
- `GET /api/v1/wallet/topup/status/{id}` —
  `Api\V1\WalletController@topupStatus`
- `POST /api/v1/wallet/topup/cancel/{id}` —
  `Api\V1\WalletController@cancelTopup`
- `POST /api/v1/wallet/transfer` —
  `Api\V1\WalletController@transfer`

## Payment

- `POST /api/v1/payments/syriatel/initiate` —
  `Api\V1\Payment\SyriatelCashController@initiate`
- `POST /api/v1/payments/syriatel/verify` —
  `Api\V1\Payment\SyriatelCashController@verify`
- `POST /api/v1/payments/mtn/initiate` —
  `Api\V1\Payment\MtnCashController@initiate`
- `POST /api/v1/payments/mtn/verify` —
  `Api\V1\Payment\MtnCashController@verify`
- `POST /api/v1/payments/cash/confirm` —
  `Api\V1\Payment\CashController@confirm`
- `POST /api/v1/payments/bank-transfer/upload` —
  `Api\V1\Payment\BankTransferController@upload`

## App / Emergency / Reviews

- `GET /api/v1/app/version` — `Api\V1\App\AppMetadataController@version`
- `GET /api/v1/app/feature-flags` —
  `Api\V1\App\AppMetadataController@featureFlags`
- `GET /api/v1/app/maintenance` —
  `Api\V1\App\AppMetadataController@maintenance`
- `GET /api/v1/app/config` — `Api\V1\App\AppMetadataController@config`
- `GET /api/v1/app/health` — `Api\V1\App\AppMetadataController@health`
- `GET /api/v1/emergency/contacts` —
  `Api\V1\Emergency\EmergencyController@contacts`
- `GET /api/v1/emergency/safety-guide` —
  `Api\V1\Emergency\EmergencyController@safetyGuide`
- `GET /api/v1/reviews/guidelines` —
  `Api\V1\ReviewController@guidelines`

---

## Deprecated paths (kept temporarily as aliases — remove after Sprint 3)

- `GET /api/v1/geography/cities/popular` → use
  `GET /api/v1/cities`.
- `GET /api/v1/geography/cities/{id}` → use
  `GET /api/v1/cities/{id}`.
- `GET /api/v1/geography/venues/clusters` → use
  `GET /api/v1/venues/clusters`.
- `GET /api/v1/geography/countries`,
  `/api/v1/geography/countries/{iso2}/states`,
  `/api/v1/geography/states/{stateId}/cities`,
  `POST /api/v1/geography/detect` — kept for now; the spec doesn't
  surface them under canonical names, but the dashboard still calls
  them.

## Missing routes (gaps for future sprints)

- `POST /api/v1/auth/register` — Sprint 3 (auth-flow reconciliation;
  spec mandates it but the codebase intentionally onboards via
  OTP-only).
- `GET /api/v1/venues/by-bounds` — Sprint 6 (Maps).
- `GET /api/v1/conversations`, `/api/v1/conversations/{id}`,
  `/api/v1/conversations/{id}/messages`,
  `POST /api/v1/messages`, `POST /api/v1/messages/{id}/mark-read`,
  `POST /api/v1/pusher/auth`, `/api/v1/conversations/{id}/mute`,
  `/api/v1/conversations/{id}/leave`,
  `/api/v1/chat/unread-summary` — Sprint 7 (Chat / Pusher).
- `POST /api/v1/teams/{id}/leave`, `POST /api/v1/teams/{id}/kick`,
  `POST /api/v1/teams/{id}/transfer-captain`,
  `POST /api/v1/teams/{id}/invite`,
  `GET /api/v1/teams/invite/{code}` — later sprint (Teams).
- `GET /api/v1/sports-profile/me`,
  `GET /api/v1/sports-profile/weekly-activity` — later sprint
  (Sports Profile).
- `GET /api/v1/coupons/my`, `/api/v1/coupons/{id}`,
  `/api/v1/coupons/validate`, `/api/v1/coupons/redeem` — Sprint 4
  (Coupons; tracked under Promotions in current code).
